---
clip-bridge: "minor:refactor"
---

Remove legacy public backend state and event shims, expose only the asynchronous run entry point, narrow dependency features, and declare the stable Rust toolchain.
